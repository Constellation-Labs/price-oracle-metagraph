environment = "dev"
intervals {
    poll = 60 seconds
    storage = 60 days
}
price-feeds = [
    {
        # empty currency-id means DAG
        # currency-id = null
        gateio-ticker = "DAG_USDT"
        kucoin-ticker = "DAG-USDT"
        mexc-ticker = "DAGUSDT"
    }
]
